version: "3.8"

services:
  mycv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mycv-app
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - mycv-network

networks:
  mycv-network:
    driver: bridge
